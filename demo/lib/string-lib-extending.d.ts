declare interface String {

  /**
   * ordinalize
   * @returns {string}
   * @description Convert a number in string format to an ordinal number
   */
  ordinalize(this: string): string; // Adding an interface instead of a namespace
}
